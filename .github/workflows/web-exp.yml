name: WEB Experiemental

on:
  workflow_dispatch:

jobs:
  build_and_deploy:
    name: Build and Deploy
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v3
        with:
          fetch-depth: 2
          lfs: true

      - name: Setup Node.js environment
        uses: actions/setup-node@v3
        with:
          node-version: 16.15.0
          cache: "yarn"

      - name: Generate environment variables
        run: |
          touch .env
          echo NEXT_PUBLIC_API_URL=${{ vars.NEXT_PUBLIC_API_URL_EXP }} >> .env

      - name: Install dependencies
        run: yarn

      - name: Generate Prisma Client
        run: yarn db:generate

      - name: Build WEB
        run: yarn build:web

      - name: Create WEB build directory
        run: cp -r dist/apps/web build

      - name: Stop current application
        uses: appleboy/ssh-action@master
        with:
          host: ${{ vars.DEPLOY_HOST_EXP }}
          username: ${{ vars.DEPLOY_USER_EXP }}
          password: ${{ vars.DEPLOY_PASSWORD_EXP }}
          port: ${{ vars.DEPLOY_PORT_EXP }}
          script: |
            cd /home/deployer/exp/web/
            pm2 delete WEB-Exp || true

      - name: Upload files
        uses: easingthemes/ssh-deploy@main
        env:
          SSH_PRIVATE_KEY: ${{ vars.DEPLOY_KEY_EXP }}
          SOURCE: "build/"
          REMOTE_HOST: ${{ vars.DEPLOY_HOST_EXP }}
          REMOTE_USER: ${{ vars.DEPLOY_USER_EXP }}
          REMOTE_PORT: ${{ vars.DEPLOY_PORT_EXP }}
          TARGET: "/home/deployer/exp/web"

      - name: Install remote packages
        uses: appleboy/ssh-action@master
        with:
          host: ${{ vars.DEPLOY_HOST_EXP }}
          username: ${{ vars.DEPLOY_USER_EXP }}
          password: ${{ vars.DEPLOY_PASSWORD_EXP }}
          port: ${{ vars.DEPLOY_PORT_EXP }}
          script: |
            cd /home/deployer/exp/web/
            yarn

      - name: Start application
        uses: appleboy/ssh-action@master
        with:
          host: ${{ vars.DEPLOY_HOST_EXP }}
          username: ${{ vars.DEPLOY_USER_EXP }}
          password: ${{ vars.DEPLOY_PASSWORD_EXP }}
          port: ${{ vars.DEPLOY_PORT_EXP }}
          script: |
            cd /home/deployer/exp/web/
            pm2 start yarn --name WEB-Exp -- start -p 3002
